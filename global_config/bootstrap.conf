#
#  Bootstrapping configuration.
#
#

define BASE                                      "${ccd}/.."
define CONFIG_BASE                               "${ccd}"

include-file environment.conf
include-file hosts.conf
